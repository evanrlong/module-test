{
  "name": "module-test-example-basic",
  "version": "0.1.0",
  "description": "Test module-test functionality",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel ./lib --out-dir ./dist",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "@evanrlong/module-test": "^0.1.0"
  }
}
